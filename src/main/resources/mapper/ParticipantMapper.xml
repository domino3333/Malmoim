<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.malmoim.mapper.ParticipantMapper">

    <insert id="insertParticipant"
            useGeneratedKeys="true"
            keyProperty="no"
            keyColumn="no">
        insert into participant(nickname, room_no, created_at)
        values (#{nickname}, #{roomNo}, NOW())
    </insert>

    <select id="isParticipantOfThisRoom" resultType="integer">
        select exists (
            select 1
            from participant
            where no = #{participantNo}
              and room_no = #{roomNo}
        )
    </select>

</mapper>
